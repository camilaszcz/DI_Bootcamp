//1

//no stars before the loop
//add an initial string outside the loop to be logged and as we do the loop it is an addition and not a new one.
let pattern= "";
// declare a const to be added every loop
const star="*";

for(let counter = 1; counter<=6; counter++){
    pattern+=star;
console.log(pattern)
}